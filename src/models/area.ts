export interface RawArea {
  id: number;
  name: string;
  description?: string;
  image_url?: string;
}

export interface Area {
  id: number;
  name: string;
  description?: string;
  imageUrl?: string;
}
